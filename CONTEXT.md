# CONTEXT.md — 日记软件术语表

> 本文件只收录已敲定的领域术语与语言约束，不含实现细节。

## 项目定位

- **日记软件**：一款安卓端日记应用，数据托管在用户自己的服务器上。
- **单用户**：仅供个人使用，无账号系统、无多用户数据隔离。（已确认，会话 Q1）

## 领域术语

### 日记条目 (Diary Entry)
一篇日记记录，由以下字段构成（已确认，会话 Q2，字段清单在会话 Q22/23 修正）：
- **标题**：可选，短文本
- **正文**：必选，主体文字；**图片是正文的一部分**，内嵌于正文中，而非独立的附件列表（已确认，会话 Q3）
- **日记日期**：日记归属的日期，编辑时可通过日期选择器补写到任意一天；用于展示、排序与日历视图打点。**每天最多一篇日记**：某天已有日记时新建会直接打开那篇的编辑，日期补写到已有日记的日期会提示冲突（已确认，会话 Q28）。**默认日期规则**：凌晨 4 点前新建的日记默认归属前一天（已确认，会话 Q29）。
- **心情**：可选，如 开心/难过 等（圆形图标行点选）
- **天气**：可选，如 晴/雨 等（圆形图标行点选）（已确认，会话 Q23）
- **同步时间戳**：客户端生成的修改时间，供后写覆盖与增量同步使用；用户不可见，与日记日期相互独立。（已确认，会话 Q23）
- ~~标签~~：**已否决**（会话 Q22），日记条目不含标签字段；检索仅靠时间、全文搜索与心情。

## 检索

- **全文搜索**：搜索框输入关键词，匹配日记条目的标题与正文文字部分；图片内容不参与搜索。（已确认，会话 Q18）

## 数据同步

- **离线优先（offline-first）**：客户端本地存有完整数据，离线可写可看，联网后与服务器同步；服务器作为备份与换机恢复来源。（已确认，会话 Q3）
- **标识与时间戳由客户端生成**：日记条目 ID 由客户端生成（如 UUID），时间戳取手机本地时间；服务器不分配标识，只做存储与转发。（已确认，会话 Q4）
- **后写覆盖（Last-Write-Wins）**：同一篇日记条目在两端都被修改时，以修改时间较新者为准，旧版本直接丢弃，不做合并。（已确认，会话 Q5）
- **软删除（墓碑）**：删除日记条目并非物理删除，而是标记为已删除并连同删除时间一起同步，保证已删条目不会在其他设备/换机恢复时复活。（已确认，会话 Q6）
- **增量同步**：客户端记录上次同步的水位，之后只交换自该点以来的变更（新增/修改/删除），而非每次都全量传输。（已确认，会话 Q9）服务器为每条变更分配单调递增的**变更序号（seq）**，客户端拉取以 seq 为水位——不依赖设备时钟，任何时间戳的服务器变更都不会漏拉；本地推送以时间戳水位过滤。（协议修正，会话 Q27）
- **图片外置**：正文中以标记引用图片，图片二进制独立于条目文本传输与存储；图片在逻辑上仍是正文的组成部分。（已确认，会话 Q8）
- **同步时机**：打开 App 时触发一次同步；编辑完日记条目保存后立即触发。不做常驻后台同步。（已确认，会话 Q14）
- **图片压缩**：客户端在保存/上传前压缩图片（最长边约 2000px、JPEG 质量约 85%），并生成缩略图用于列表展示；压缩版是唯一版本，不保留原始照片。（已确认，会话 Q15/16）
- **换机恢复**：新设备首次连接时填写服务器地址与 Token，从服务器执行一次全量拉取完成恢复；增量同步以全量拉取为基线。（已确认，会话 Q17）
- **双端互为备份**：手机本地持有完整数据、服务器持有一份完整副本，互为恢复来源；不额外做服务器侧备份脚本，接受双端同时损坏的极端风险。（已确认，会话 Q19）

## 服务器

- **自托管**：运行在用户自己的常开 Linux 机器上。（已确认，会话 Q7）
- **技术栈**：Go + SQLite。（已确认，会话 Q10）
- **访问保护**：客户端与服务器共享 API Token（随机长字符串），请求时携带校验；服务器上启用 HTTPS。（已确认，会话 Q11）

## 客户端

- **技术栈**：Kotlin + Jetpack Compose，原生安卓；不考虑 iOS。（已确认，会话 Q12）

## 界面结构

- **导航**：底部四 Tab：日记 / 日历 / 我的 / 设置；全文搜索入口为日记列表页顶部的搜索按钮。（已确认，会话 Q20-22）
- **列表页**：日记条目按日记日期倒序排列，每条显示首图缩略图 + 标题/正文摘要 + 日期。（已确认，会话 Q21）
- **日历页**：月视图，有日记的日期打点标记；**选中日期后日历下方展示当天的日记卡片**，点卡片打开阅读弹窗。（已确认，会话 Q21，布局修正于会话 Q30）
- **我的页**：统计概览（总篇数、连续天数、心情分布等）+ 数据管理（导出、存储占用等）；统计卡下方有媒体库入口卡片，预览最近几张图片。（已确认，会话 Q22，媒体库入口补于会话 Q31）
- **媒体库**：点击"我的"页的媒体库卡片进入的独立页面，瀑布流展示全部日记条目的图片，按日记日期倒序；卡片标注所属日记条目标题与文件体积，点击缩略图进入全屏预览，预览内可跳转所属日记条目或原样导出图片（WebP）；纯只读——删除图片只能在编辑页做；未上传成功的图片照常显示。（已确认，会话 Q31）
- **编辑页**：顶栏 取消 | 标题输入框 | 蓝色胶囊保存按钮；正文输入区；底部工具栏五个图标：图片 / 时间戳 / 心情 / 天气 / 日期——心情与天气为圆形图标行点选，日期为翻页选择器可补写任意一天。（已确认，会话 Q23）
- **阅读（弹窗）**：点击列表卡片或日历卡片以**底部弹窗**展开阅读视图：大标题、心情图标、图片大图、正文；操作：编辑 / 删除 / 导出为图片。不做全屏详情页。（已确认，会话 Q30）
- **外观**：Material You 动态取色 + 自动深浅色模式。（已确认，会话 Q25）

## 语言约束

- 不使用"帖子 / post / 记录"等词指代日记条目，统一用 **日记条目**。
- "同步"指客户端与服务器之间的数据交换，不指任何其他含义。
